# ☁️ perfecto-ai-digitalocean

<div align="center">

**Perfecto-AI 파이프라인을 DigitalOcean Droplet 위에서**  
**“사람 손 안 타고, 시간 맞춰 YouTube 쇼츠 올리는”**  
**Headless Auto Publisher**

---

![Python](https://img.shields.io/badge/Python-3.10+-3776AB?logo=python&logoColor=white)
![Ubuntu](https://img.shields.io/badge/Ubuntu-20.04%20%7C%2022.04-E95420?logo=ubuntu&logoColor=white)
![Cron](https://img.shields.io/badge/Scheduler-cron-informational)
![Cloud](https://img.shields.io/badge/Cloud-DigitalOcean-0080FF?logo=digitalocean&logoColor=white)
![YouTube](https://img.shields.io/badge/Auto%20Upload-YouTube-FF0000?logo=youtube&logoColor=white)
![Status](https://img.shields.io/badge/status-Archived%20%7C%20Verified-green)

</div>

---

## 🧾 한 줄 설명

> **이 레포는 “말로만 배포해봤다”가 아니다.**  
> DigitalOcean Ubuntu에서 `cron`으로 걸어두고,  
> **정해둔 시간에 YouTube 쇼츠가 실제로 자동 생성 → 자동 업로드되는 것까지 검증한 Headless 러너 코드**다.

---

## 🌐 이 레포는 무엇인가?

이 레포는 **웹 UI가 있는 Streamlit 버전 [`Perfacto-AI`](https://github.com/Lova-clover/Perfacto-AI)가 아니라,

- DigitalOcean Ubuntu Droplet 위에서
- `cron` + **Python 스크립트**로
- **정해진 시각마다 쇼츠 영상을 만들고, YouTube에 업로드**하기 위해 분리한  
  **배포/운영 전용 레포지토리**다.

요약하면 한 줄이다:

> **“새벽 7시에 알아서 영상 올라가는 채널”을 만들기 위해 실제로 돌려본 코드 스냅샷.**

로컬에서만 놀던 걸 넘어서,

- 클라우드 서버에 올리고
- 스케줄링을 걸고
- 업로드까지 붙여서
- “제 시간에 진짜 올라가는지” 확인하는 데 성공한 버전이다.

---

## 🧱 역할 & 구성 개요

### 이 레포가 담당하는 역할

- **완성본 영상 생성**
  - Perfecto-AI의 **이미지 + 자막 + TTS + BGM + 9:16 합성 파이프라인**을 그대로 사용
- **Headless 실행**
  - Streamlit UI 없이, **Python 스크립트만으로 실행** 가능한 구조
- **스케줄링**
  - `cron`이 `runner.py` / `run_*.py`를 정해진 시각에 호출
- **YouTube 업로드**
  - `upload.py`가 YouTube Data API로 **완성된 mp4를 자동 업로드**

### 주요 스크립트 역할

- `runner.py`  
  → `job_config.yaml`을 읽어서 **여러 잡(job)을 한 번에 실행**하는 메인 배치 러너

- `run_img_voice.py`  
  → 이미지 + 내레이션(TTS) + BGM이 들어간 **일반 쇼츠 영상 자동 생성 & 업로드**

- `run_img_muted.py`  
  → **음성 없이 이미지 + 자막 + BGM**만 들어간 **무성 감성 영상용 러너**

- `run_emotional.py`  
  → 감성 텍스트/문장 위주 시나리오를 위한 **감성 버전 자동 생성 러너**

- `job_config.yaml`  
  → 어떤 **페르소나 / 스타일 / 언어 / BGM / 업로드 여부**로  
    어떤 잡을 어떤 폴더에 출력할지 정의

- `upload.py`  
  → 완성된 mp4 파일을 **지정된 YouTube 채널에 업로드**하는 모듈

- `video_maker.py`, `generate_timed_segments.py`, `elevenlabs_tts.py`,  
  `image_generator.py`, `text_scraper.py` …  
  → Perfecto-AI 본 프로젝트의 핵심 모듈을 **서버에서 바로 호출 가능한 형태로 정리**한 코드들

---

## 🧩 Perfecto-AI 본 레포와의 관계

- [`Perfacto-AI`](https://github.com/Lova-clover/Perfacto-AI)  
  → **사람이 브라우저로 조작하는 Streamlit 웹앱 (Interactive)**

- `perfecto-ai-digitalocean`  
  → **서버에서 혼자 돌아가는 Headless / Batch Runner (Non-interactive)**

**공통점**

- LLM 기반 스크립트 생성
- ElevenLabs / Amazon Polly TTS
- BGM + 자막 + 이미지 + 9:16 세로 영상 합성
- YouTube 업로드 기능

**차이점**

- 이 레포에는 **UI가 없다.**
- 대신:
  - DigitalOcean Ubuntu 환경에 맞춘 패키지/의존성
  - `job_config.yaml` 기반 잡 정의
  - `cron`용 예시
  를 포함해 **“실제 서버에서 돌릴 때 필요한 것들”**에 초점을 맞춘 설계다.

운영 측면에서 이 레포가 의미 있는 이유:

- “로컬에서만 돌려봤어요”가 아니라  
  **“클라우드에서 스케줄링 걸고, 새벽에 진짜 영상 올라가는 것까지 확인했다”**를 증명한다.

---

## ⚙️ 환경 / 의존성

이 레포는 **DigitalOcean Ubuntu Droplet** 기준으로 검증했다.

### 시스템 요구사항

- Ubuntu 20.04 / 22.04 (DigitalOcean Droplet)
- Python **3.10+**
- `ffmpeg`
- (선택) `git`, `cron`, `tmux` (원격 운영 편의를 위해 사용)

### Python 패키지

```bash
pip install --upgrade pip
pip install -r requirements.txt
````

### 시스템 패키지

Droplet 초기 세팅 시:

```bash
sudo apt update
sudo apt install -y ffmpeg git
# 추가 폰트/툴이 필요하면 packages.txt 참고
```

---

## 🔐 환경 변수 & Secrets

API 키 / 토큰은 **코드에 하드코딩하지 않고**,
환경 변수 또는 별도 secret 파일로 관리했다. (레포에 포함 X)

예시:

```bash
export OPENAI_API_KEY="sk-..."
export ELEVEN_API_KEY="eleven-..."
export GOOGLE_API_KEY="AIza..."
export AWS_ACCESS_KEY_ID="AKIA..."
export AWS_SECRET_ACCESS_KEY="..."
export AWS_REGION="ap-northeast-2"

# YouTube OAuth 토큰 JSON (문자열 그대로)
export YT_TOKEN_JSON='{ ... }'

# Pexels 이미지 검색용
export PEXELS_API_KEY="your-pexels-key"
```

실제 운영에서는:

* `.env` 파일 로드
* `systemd` 서비스의 `Environment=`
* `cron`에 `source ~/.profile` 후 실행

등 방식으로 넘겼다.

---

## 🗓️ 스케줄링 구조 (cron)

실제 DigitalOcean Droplet에서 사용한 구조는 **cron + Python** 조합이다.

### crontab 예시

```bash
crontab -e
```

```cron
# 매일 07:00에 img+voice 쇼츠 생성 & 업로드
0 7 * * * /usr/bin/python3 /root/perfecto-ai-digitalocean/run_img_voice.py >> /var/log/perfecto_ai_voice.log 2>&1

# 매일 23:00에 감성 텍스트 기반 무성 영상 생성 (업로드 X)
0 23 * * * /usr/bin/python3 /root/perfecto-ai-digitalocean/run_emotional.py >> /var/log/perfecto_ai_emotional.log 2>&1
```

또는 여러 잡을 `runner.py`로 한 번에 묶어서 돌릴 수도 있다:

```cron
# 매일 06:00에 job_config.yaml에 정의된 잡 전체 실행
0 6 * * * /usr/bin/python3 /root/perfecto-ai-digitalocean/runner.py -c job_config.yaml >> /var/log/perfecto_ai_runner.log 2>&1
```

이렇게 걸어두면:

1. 새벽에 서버가 알아서 LLM으로 대본 만들고
2. TTS + 자막 + 이미지 + 영상 합성 돌린 다음
3. YouTube에 업로드까지 끝낸 뒤
4. **아침에 눈 뜨면 채널에 새 쇼츠가 올라와 있는 상태**가 된다.

> 이 구조로 **정해둔 시간에 YouTube 영상이 실제로 자동 업로드되는 것까지 직접 확인**했다.

---

## 🧪 현재 상태 (Status)

* **역할**: Perfecto-AI의 **DigitalOcean 배포 / 스케줄링 운영 레포**
* **검증 단계**:

  * Droplet에서 `cron`으로 걸어
  * **여러 차례 자동 생성 + 자동 업로드 테스트 완료**
* **현재**:

  * 기능 확장/리팩토링은 **메인 레포(Perfacto-AI)** 기준으로 진행 중
  * 이 레포는 **“그때 실제로 어떻게 돌렸는지”를 남겨둔 운영 스냅샷**
  * 포트폴리오에서 **“클라우드 배포 + 자동화 운영 경험”**을 증명하는 용도로 사용 가능

---

## 📁 디렉터리 구조 (요약)

```bash
perfecto-ai-digitalocean/
├─ .devcontainer/              # VS Code Dev Container 설정 (원격 개발용)
├─ .streamlit/                 # (필요 시) Streamlit config (headless에선 거의 미사용)
├─ RAG/                        # RAG 관련 유틸 (문서 로딩, 캐시 등)
├─ assets/
│   └─ fonts/                  # 자막/렌더링용 폰트
├─ job_config.yaml             # 배치 잡 설정
├─ packages.txt                # OS 레벨 패키지 목록 (참고용)
├─ requirements.txt            # Python 의존성
├─ runner.py                   # 여러 잡을 한 번에 돌리는 메인 러너
├─ run_img_voice.py            # 이미지 + 보이스 + BGM 영상 자동 생성 & 업로드
├─ run_img_muted.py            # 이미지 + 자막 + BGM (무성) 영상 자동 생성
├─ run_emotional.py            # 감성 텍스트 기반 영상 자동 생성
├─ upload.py                   # YouTube 업로드 모듈
├─ video_maker.py              # 이미지 + 오디오 → mp4 생성
├─ generate_timed_segments.py  # 자막 타이밍/세그먼트 생성
├─ elevenlabs_tts.py           # ElevenLabs / Polly TTS 래퍼
├─ image_generator.py          # Pexels 기반 이미지 검색 모듈
├─ text_scraper.py             # 텍스트/스크립트용 웹 스크래핑
├─ persona.py, personas.yaml   # 페르소나 정의 및 로딩 유틸
└─ ...
```

---

## ⚠️ 사용 시 주의 (과제/공모전 복붙 금지)

> 이 레포는 **실제 운영 실험 코드**를 기록하기 위한 용도이며,
> **오픈소스 라이선스로 배포되는 프로젝트가 아니다.**

### ❌ 절대 하면 안 되는 사용

* 수업 과제 / 캡스톤 / 공모전 / 채용 과제 등에
  이 레포 코드를 **그대로 복붙해서 제출**하는 행위
* 변수명/함수명/파일명만 조금 바꾸고
  실질적으로 동일한 코드로 **본인 단독 구현인 것처럼 제출**하는 행위
* 사전 동의 없이 이 레포 전체/대부분을
  **상업 서비스/제품에 그대로 탑재**하는 행위

이런 사용은:

* 학내 규정
* 공모전/대회 규정
* 플랫폼 약관

을 동시에 위반할 수 있으며,
관련 책임은 **사용자 본인에게 전적으로 귀속**된다.

### ✅ 허용되는 사용

* 파이프라인 구조나 스케줄링 방식을 **레퍼런스로 읽어보는 것**
* 내용을 이해한 뒤, **본인 스타일로 새로 구현**해보는 것
* 개인 학습/연구/실험 용도로 Droplet에서 직접 돌려보는 것

---

## 👤 Author

* **Developer / Operator**: 한성주 (Han Seongju)

  * GitHub: [Lova-clover](https://github.com/Lova-clover)
  * Blog: [https://velog.io/@lova-clover/posts](https://velog.io/@lova-clover/posts)

> Perfecto-AI 웹앱 레포와 이 DigitalOcean 러너 레포를 같이 보면,
> **“아이디어 → 프로토타입 → 웹앱 → 클라우드 배포 → 스케줄링 운영”**까지 한 사이클을 직접 굴려본 흔적이 전부 남아있다.
